language: python
sudo: false

python:
    - "2.6"
    - "2.7"
    - "3.3"
    - "3.4"

env:
    - RUN=nosetests
    - RUN=build_sphinx

install:
    - ./.travis.install.sh

script:
    - ./.travis.script.sh
